import { InventoryRequisition } from './inventory-requisition';

describe('InventoryRequisition', () => {
  it('should create an instance', () => {
    expect(new InventoryRequisition()).toBeTruthy();
  });
});
